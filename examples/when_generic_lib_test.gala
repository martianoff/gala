package main

import (
    . "martianoff/gala/std"
    "martianoff/gala/examples/when_generic_lib"
)

func main() {
    val result = when_generic_lib.GetSession("test")
    result match {
        case Some(v) => Println(v)
        case _ => Println("none")
    }
}
