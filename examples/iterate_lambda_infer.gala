package main

import . "martianoff/gala/stream"

func main() {
    // T should be inferred as int from seed=1
    // Lambda param x should be inferred as int
    val powers = Iterate(1, (x) => x * 2)
    val first5 = powers.Take(5).ToArray()
    Println(first5)
}
