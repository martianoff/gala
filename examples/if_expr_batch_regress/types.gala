package if_expr_batch_regress

// Second file to force multi-file batch transpilation (transpile-package).
// The regression only manifests in this mode.

type Pair struct {
    Key   string
    Value string
}
