package main

import "fmt"

func identity[T any](x T) T = x

func main() {
    val x = NewImmutable(10)
    var y = identity(x) // Inferred as Immutable[int], so unwrapped to int
    fmt.Printf("y: %d\n", y)
}
